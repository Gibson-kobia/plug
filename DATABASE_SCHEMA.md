# DATABASE SCHEMA — Kenya Electronics Marketplace

**Status:** Placeholder — full DDL to be created during TASK-003 through TASK-007 as Supabase PostgreSQL migrations in `supabase/migrations/`**

The complete DDL, stored procedures, triggers, and RLS policies are formally specified in:
👉 `.trae/documents/Technical-Architecture.md` §6 Stored Procedures, §7 Triggers, §5 RLS, §4 ER Diagram.

---

## Production DDL Index (Pending Implement in Migrations)

### Migration 0001 — Roles, Auth, System Tables
**File:** `supabase/migrations/0001_init_roles_and_auth.sql` — TASK-003`
- Tables: roles (5), profiles, sync_auth_user_role_claim() trigger, audit_logs, system_settings_typed, sessions_anon, notification_preferences
- Extensions: pg_trgm, pg_cron, pgcrypto, pg_stat_statements
- JWT claim sync trigger

### Migration 0002 — Catalog Core
**File:** `supabase/migrations/0002_catalog_core.sql` — TASK-004`
- Tables: categories (parent_id tree, product_count with trigger), brands, products, product_variants (SKU UNIQUE CHECK UNIQUE NOT NULL attributes_jsonb UNIQUE(sku)
- product_images, price_history, spec_templates + spec_values jsonb
- mv_search_index materialized view with CONCURRENTLY every 15 min (GIN: tsvector + gin_trgm_ops)
- reviews (order_item_id FK NOT NULL → verified-purchase gate
- homepage_sections + homepage_section_items
- product_alerts (price-drop KES threshold + back-in-stock variant subscribe)

### Migration 0003 — Sellers & Moderation
**File:** `supabase/migrations/0003_sellers_moderation.sql` — TASK-005`
- Tables: seller_profiles, seller_verification_documents (pgp_sym_encrypt for Huduma #), listing_drafts, used_listings, used_listing_photos, moderation_queue_events, listing_reports, listing_enquiries

### Migration 0004 — Orders, Stock & Finance
**File:** `supabase/migrations/0004_orders_stock_finance.sql` — TASK-006`
- **CART:** carts, cart_items
- **RES:** reservations + release_expired_reservations_for() + cron 1 min
- **STOCK INTEGRITY SP:** reserve_variant() (release inline sweep → SELECT…FOR UPDATE SKIP LOCKED → CHECK stock ≥ qty → −qty → INSERT reservations → INSERT inventory_transactions reason=reserve_hold
- **INTEGRITY LEDGER:** inventory_transactions (immutable reason enum
- **FINANCE:** commission_rules, seller_ledger_entries (running_balance_after_kes double-entry)
- **COUPONS:** coupons + coupon_redemptions
- **ORDERS:** orders, order_items, sign_order_ref() HMAC SHA-256 8-char sig SP, order_fulfillment_groups (1 per seller/platform), order_fulfillments (partner/tracking_no/driver/proof_of_delivery/signature/delivered_at), order_events 8-state FSM, disputes, order_cancellations, return_requests, refunds, order_payments
- **pg_cron jobs: reservation_sweeper 1 min, order_sweeper 2 min, category_product_count_sweeper 10 min

### Migration 0005 — Fulfillment & Locations
**File:** `supabase/migrations/0005_fulfillment.sql` — TASK-007`
- Tables: delivery_zones (Nairobi 8, outskirts, nationwide), pickup_locations (address_structured, operating_hours_jsonb, lat, lng, active

### Migration 0006 — RLS Final Pass & Seed
**File:** `supabase/migrations/0006_rls_final.sql` — TASK-008
- Every table: enable RLS; policy-by-table for buyer/seller/moderator/admin/super_admin policies
- JWT app_metadata.role direct claim from JWT role_claim direct use
- Every policy: `auth.jwt() -> 'app_metadata' ->> 'role' in ('admin','super_admin')`

---

## ER Diagram Summary Cardinalities (from Technical-Architecture.md §4 ERD)

```mermaid
erDiagram
    categories ||--o{ products : ""
    products ||--o{ product_variants : SKU
    product_variants ||--o{ inventory_transactions : immutable delta log
    products ||--o{ reviews : order_item_id FK NOT NULL
    seller_profiles ||--o{ products : "platform managed = null or seller_id
    seller_profiles ||--o{ seller_verification_documents : "KYC + Huduma encrypt
    orders ||--o{ order_fulfillment_groups : split per seller/platform split
    order_fulfillment_groups ||--o{ order_items : items
    order_fulfillment_groups ||--o{ order_fulfillments : 1:N deliveries
    orders ||--o{ order_events : FSM 8-state transitions
    seller_profiles ||--o{ seller_ledger_entries : 1:N ledger running balance
    commission_rules ||--o{ seller_ledger_entries : ""
    coupons ||--o{ coupon_redemptions : per order
    orders ||--o{ disputes : 1:N
    products ||--o{ product_alerts : price-drop back-in-stock
```

---

**Do not edit this file manually. It will be replaced with actual DDL SQL during migration implementation (TASK-003 through -008) and kept as migration files created in migrations folder.
